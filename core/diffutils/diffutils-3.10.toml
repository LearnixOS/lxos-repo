[package]

name = "diffutils"
version = "3.10"
author = "saladtoes"
type = "curl"
src = ["https://ftp.gnu.org/gnu/diffutils/diffutils-3.10.tar.xz"],

[build]

compile = [
        "./configure --prefix=/usr",
        "make",
]

install = [
"make install"
]


