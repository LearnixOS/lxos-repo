[package]

name = "automake"
version = "1.17"
author = "saladtoes"
type = "curl"
src = ["https://ftp.gnu.org/gnu/automake/automake-1.17.tar.xz"]

[build]

compile = [
        "./configure --prefix=/usr --docdir=/usr/share/doc/automake-1.17",
        "make"
]

install = [
	"make install"
]
